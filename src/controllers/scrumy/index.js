const Scrumy = require('../../models/scrumy.model');
const catchAsync = require('./../../utils/catchAsync');
const factory = require('../Helpers/handlerFactory');

exports.invite = catchAsync(async (req,res, next) => {
//coming soon
});